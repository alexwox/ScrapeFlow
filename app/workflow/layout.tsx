import React from 'react'


function layout({children}:{children:React.ReactNode} ) {
  return (
    
    <div>layout<{children}:{children:React.ReactNode} /div>
 
  )
}

export default layout{children}:{children:React.ReactNode} 